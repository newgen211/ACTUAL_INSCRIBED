import { TextField } from '@mui/material';


export default function SearchBar() {

    return (

        <TextField
            fullWidth
            id='search-bar'
            name='search-bar'
            label="Search..."

        />

    );

}